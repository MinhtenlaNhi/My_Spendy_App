<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Viền -->
    <stroke
        android:width="2dp"
        android:color="#A6A6A6" /> <!-- màu viền -->

    <!-- Góc bo -->
    <corners android:radius="10dp"/>

    <!-- Màu nền -->
    <solid android:color="#FFFFFF"/>

    <!-- Padding bên trong -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
